description: Show pagination on characters list page
tags:
  - all

bucketBy: userId

environments:
  preview:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  development:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  staging:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  production:
    rules:
      - key: "1"
        segments:
          - poland
        percentage: 100

      - key: "2"
        segments: "*" # everyone else
        percentage: 0
