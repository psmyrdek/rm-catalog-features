description: Show pagination on characters list page
tags:
  - all

bucketBy: country

environments:
  preview:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  development:
    rules:
      - key: "1"
        segments: "*"
        percentage: 100

  staging:
    rules:
      - key: "1"
        segments: "*"
        percentage: 0

  production:
    rules:
      - key: "1"
        segments: "*"
        percentage: 0
